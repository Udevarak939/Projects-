name: customer360
version: '1.0'
config-version: 2
profile: customer360
model-paths: [models]
test-paths: [tests]
clean-targets: [target, dbt_packages]
models:
  customer360:
    staging:
      +materialized: view
    marts:
      +materialized: table
